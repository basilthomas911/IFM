# G2-030-034 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-030` through `G2-034`: order/trade creation, state change, and child-first removal |
| Decision | **Passed** |
| Run ID | `20260819-025747-ab853f5ecb504d80bb54fc88b046db5c` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-027 and G2-030 through G2-034: 13 passed, 0 failed, 0 blocked |
| Process-test duration | 6 minutes 33 seconds |
| Fund fixture | Fund `2201`, `IFM G2 Automation Fund`, non-production |
| Value date | `2026-08-19` |
| Cleanup | Succeeded; typed queries confirmed the captured zero-order/zero-trade baseline |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development actor backend and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured typed order/trade baselines, and armed the exact-ID command observer before mutation. The isolated slice selected the synthetic source only for unrelated streaming activity. The Trade UI, NATS clients, domain actors and events, Scylla projections, and typed queries remained the real Development implementations. The mandatory startup rollover check still resolved and persisted current ES/VX contracts through Databento before the host became ready.

G2-030 created order `2801` under fund `2201` with reference `G2-AB853F5E-Order` and base contract `ES20260918`. `OrderAddedToFundEvent` and `OrderAddedToFundCompleteEvent` correlated under command ID `f5ac9d09-2d6e-42b4-aa85-044315d3b9ee`. The complete-event payload, typed durable query, and visible Trade Orders row agreed.

G2-031 created Short Iron Condor trade `501` with reference `G2-AB853F5E-Trade`. The modal-displayed identifier, submitted command payload, `TradeAddedToFundOrderEvent`, `TradeAddedToFundOrderCompleteEvent`, durable projection, and refreshed list row all used that same identity under command ID `514d3653-3c33-4b6d-ba1b-e7637e89d3f6`. A new trade with an ordinary operator reference now initializes four unset option legs for later editing instead of assuming that its reference already encodes four contracts.

G2-032 changed trade `2801:501` from `NewTrade` to `OrderSubmitted`. `FundOrderTradeStateChangedEvent` and `FundOrderTradeStateChangedCompleteEvent` correlated under command ID `5d3e56f5-8b9c-4819-a152-2dd877acb171`; the terminal payload, durable row, target-state selector, and visible state column agreed.

G2-033 removed child trade `2801:501` under command ID `8f25221c-c6d7-4f78-bb50-ecf1e9b1df74`. The source and `TradeRemovedFromFundOrderCompleteEvent` correlated exactly, and both typed query and refreshed UI proved the child absent while order `2801` remained. G2-034 then removed order `2801` under command ID `4cfaff0e-b499-4548-80e4-a4d4d6aa132b`; `OrderRemovedFromFundEvent` and `OrderRemovedFromFundCompleteEvent` correlated exactly, and the durable/UI state proved the parent absent with no run-owned trades.

Acceptance exposed and corrected four UI integration defects: required selector/date accessibility was missing, the selected Iron Condor detail control could initialize repeatedly during one refresh, an ordinary trade reference could cause four-leg template initialization to index an empty parse result, and the parent editor allocated a second trade ID after the modal had already displayed one. The Databento session-statistics realtime source was also routed through the registered `TickAggregationRealtime` actor rather than a nonexistent actor name. Focused regressions plus the full 190-test presentation suite and 480-test market-data feed suite passed.

The finally path required no compensating command after the accepted run because G2-033 and G2-034 performed the intended child-first cleanup. It re-queried `orders=0` and `trades=0`; the Trade Orders window, listeners, desktop, and harness-owned API then stopped normally. Accepted logs contained no error or fatal entry, no rejected realtime actor message, and no Iron Condor load-error dialog.

This decision accepts G2-030 through G2-034 in addition to the prior accepted G2-001 through G2-029 slices. It does not claim G2-035 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, and cleanup evidence remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260819-025747-ab853f5ecb504d80bb54fc88b046db5c-Development/`
