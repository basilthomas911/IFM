# G2-008–009 accepted result — Development — 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-008` through `G2-009`: operator market-data feed start and stop |
| Decision | **Passed** |
| Run ID | `20260818-124944-187341eecc5447de8e5cbf9a2fc69e7d` |
| Environment | `Development` |
| Automated result | Complete G2-001–009 run: 9 passed, 0 failed, 0 blocked |
| Process-test duration | 2 minutes 13 seconds |
| Cleanup | Succeeded |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development API and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured the reversible baseline, and armed the shared command observer before the feed actions. The initialized shell reported the startup feed as active, so the test first normalized it through the same UI stop control. That normalization command, `528301c1-e39f-40f4-aaed-b45f148bd749`, produced matching stopped source and stopped-complete events.

G2-008 then invoked `Start Market Feed` in the real shell. The resulting command `4a2b2936-38a6-4e0f-a04a-118e3f96e5c4` produced `MarketDataFeedStartedEvent` and, 33.275 seconds later, the exact-ID `MarketDataFeedStartedCompleteEvent`. Only after that terminal completion did the enabled UI action change to `Stop Market Feed`, proving the visible active state was backend-confirmed rather than inferred from command acceptance.

G2-009 invoked `Stop Market Feed`. Command `eca81230-35ec-457f-b6f9-65ca484f1d3d` produced the exact stopped source and `MarketDataFeedStoppedCompleteEvent`; the terminal arrived before the UI exposed `Start Market Feed` and accepted the inactive state. The shell closed normally, the event listeners stopped, cleanup reported no failures, UI stderr was empty, and API stderr contained only four successful Databento live authentication/start pairs for the two initial and two restarted dataset sessions. The harness-owned API required its normal bounded forced termination after the graceful desktop close; no product process remained after cleanup.

During verification, a preceding run exposed that the live XCBF subscription could return a different provider instrument ID than the metadata lookup for the same raw VX contract. The accepted implementation now binds that live identity through the validated dataset/date/raw-symbol mapping and rejects any conflicting contract mapping. Focused unit coverage proves the live-symbol binding and aggregator startup behavior.

This decision accepts G2-008 and G2-009 in addition to the prior accepted G2-001–007 slice. It does not claim G2-010 through G2-038. Raw screenshots, UI Automation trees, logs, exact source/terminal events, process evidence, and the machine-readable result remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-124944-187341eecc5447de8e5cbf9a2fc69e7d-Development/`
