# G2-035-038 accepted result - Development - 2026-08-20

| Field | Result |
|---|---|
| Gate slice | `G2-035` through `G2-038`: EOD, database backup, restoration, and shutdown |
| Decision | **Passed** |
| Run ID | `20260820-110042-6d2c12b3e92f4adca4958beb2510afe3` |
| Environment | `Development` |
| Automated result | 4 passed, 0 failed, 0 blocked |
| Process-test duration | 4 minutes 6 seconds |
| Cleanup | Succeeded; desktop, API, backup host, listeners, query session, and run-owned backup root all stopped or were removed |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development actor backend and WinForms desktop. G2-035 created disposable order `8001` and trade `2401` under non-production fund `2201`, ran the supported ES end-of-day dialog, and observed `EndOfDayFundTransactionProcessedCompleteEvent` under exact initiating command `31f9adb6-3099-4035-8623-0e7b19ff2b2a`. The durable unrealized P&L was zero for the deterministic fixture and the dialog closed normally.

G2-036 opened the real System Administration screen and submitted a full `LocalWorkstation` dry-run for allowlisted protection set `core-postgresql`. Operation `2572853a6bdc4c6099463eb597a37e81` reached `Completed/Succeeded`; the domain events, typed projection, visible status row, and one run-owned execution journal agreed.

G2-037 removed the EOD trade before its parent order through the public UI command path. Selection-specific accessible action names ensure automation cannot delete a neighboring order after an asynchronous list refresh. The reusable fund returned to balance `100000.00`; its two append-only EOD rows netted to zero; the captured order/trade, yield-curve, and economic-calendar baselines matched; the one backup artifact and its run-owned root were removed.

G2-038 closed all secondary windows and the main shell normally. The desktop required no forced termination, the harness-owned API and database-backup host exited, NATS command observation and typed query connections were disposed, and no run-owned backup root remained.

This result completes G2. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact event observations, and cleanup evidence remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260820-110042-6d2c12b3e92f4adca4958beb2510afe3-Development/`
