# G2-024-026 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-024` through `G2-026`: lookup maintenance |
| Decision | **Passed** |
| Run ID | `20260818-191132-8c41fd5c72034044b5eda0581c26b54d` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-024 through G2-026: 10 passed, 0 failed, 0 blocked |
| Process-test duration | 1 minute 28 seconds |
| Lookup fixture | `G2-8C41FD5C-Lookup`, order ID `0` |
| Cleanup | Succeeded; the public Reference command API restored the captured empty lookup partition |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development API and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured the typed reversible baseline, and armed the shared command-event observer before mutation. The unique run prefix owned no pre-existing lookup rows. The fixture deliberately used a brand-new lookup name containing one order-zero value because lookup storage identity is name plus order ID and removal reorders remaining values within that partition; removing the sole run-owned row therefore could not affect operator data.

For G2-024, the real lookup editor submitted `G2-8C41FD5C-A` with description `G2-8C41FD5C lookup added`. `LookupTypeAddedEvent` and `LookupTypeAddedCompleteEvent` carried command ID `3421395e-03b8-406d-a760-aeb339d3d215`. The typed lookup-partition query returned exactly one row with every expected business field, and the refreshed editor displayed the new lookup name and short code.

For G2-025, the editor preserved lookup identity `G2-8C41FD5C-Lookup : 0` while changing the short code to `G2-8C41FD5C-B` and description to `G2-8C41FD5C lookup changed`. `LookupTypeChangedEvent` and `LookupTypeChangedCompleteEvent` correlated under command ID `9abc0d45-5eb4-45f7-8d97-0a1bcec7cbcf`. Durable and visible state contained the changed value exactly once, and the original short code was absent.

For G2-026, the real confirmation dialog submitted removal. `LookupTypeRemovedEvent` and `LookupTypeRemovedCompleteEvent` correlated under command ID `8105f7e9-206c-4174-94dd-4c6a8ec23ece`. The typed partition query returned zero rows, and the refreshed lookup-name selector no longer contained the isolated partition.

The editor's terminal listener was active before each submission and accepted only a complete/fail event with the exact command ID. A successful complete triggered a fresh typed durable query before callbacks repainted the UI; a fail or unobserved outcome could not be reported as success. Focused presentation tests also cover unrelated terminal events, an event arriving before the command response, failure without refresh, listener lifecycle, and removal refresh.

The manual removal already restored the empty lookup baseline. The finally-path cleanup re-queried the partition and confirmed that no compensating mutation was required. The Reference window, lookup listener, shared command observer, desktop, and harness-owned API all stopped normally, and cleanup succeeded.

This decision accepts G2-024 through G2-026 in addition to the prior accepted G2-001 through G2-023 slices. It does not claim G2-027 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, cleanup evidence, and the machine-readable result remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-191132-8c41fd5c72034044b5eda0581c26b54d-Development/`
