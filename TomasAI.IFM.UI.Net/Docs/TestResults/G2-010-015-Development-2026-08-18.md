# G2-010-015 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-010` through `G2-015`: futures contract and futures-option add/change/remove |
| Decision | **Passed** |
| Run ID | `20260818-142242-7667216be70b422a88b3bf4cc3f63c92` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-010 through G2-015: 13 passed, 0 failed, 0 blocked |
| Process-test duration | 2 minutes 38 seconds |
| Cleanup | Succeeded; no compensating command was needed |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development API and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured an empty baseline for the exact far-future run-owned fixture, and armed the shared event observer before any mutation. This isolated slice relies on the separately accepted G2-008 through G2-009 feed decision; it does not repeat the feed stop/start journey.

The UI added futures contract `ES20470616`, changed its description to `G2-7667216B futures changed`, and reloaded the editor to prove the stored description was rendered. It then added call option `ES20470616C3390`, changed its description to `G2-7667216B option changed`, and reloaded the option editor to prove durable/UI agreement. Finally, it removed the option before its parent futures contract. Typed NATS queries confirmed presence, changed values, and absence after each applicable operation.

Six distinct command IDs produced 12 observed events: one source event and one successful exact-ID terminal event for each add, change, and remove command. The run recorded screenshots and UI Automation trees for every accepted step. The cleanup query found neither fixture, so no compensating removal was issued. The Market Data window, shell, event listeners, desktop, and harness-owned API then stopped within their bounded cleanup policy; no IFM product process remained.

Verification also corrected product behavior exposed by the acceptance journey: the futures editor now renders the durable description instead of regenerating it, Add/Change fields are editable when appropriate, editor action buttons remain disabled until lookup/query loading completes, vendor venue and multiplier values map to authoritative UI lookups, and a completed futures mutation refreshes button state without re-entering asynchronous editor selection. Futures-command presentation now waits for the exact terminal complete/fail event before re-querying.

This decision accepts G2-010 through G2-015 in addition to the prior accepted G2-001 through G2-009 slices. It does not claim G2-016 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, fixture policy, process evidence, and the machine-readable result remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-142242-7667216be70b422a88b3bf4cc3f63c92-Development/`
