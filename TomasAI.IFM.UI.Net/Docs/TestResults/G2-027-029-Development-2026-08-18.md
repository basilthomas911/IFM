# G2-027-029 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-027` through `G2-029`: reusable fund fixture and compensating transactions |
| Decision | **Passed** |
| Run ID | `20260818-224604-7187833ac9354523bb3bcba7733129a7` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-027 through G2-029: 10 passed, 0 failed, 0 blocked |
| Process-test duration | 1 minute 18 seconds |
| Fund fixture | Fund `2201`, `IFM G2 Automation Fund`, non-production |
| Value date | `2026-08-19` |
| Cleanup | Succeeded; the equal UI withdrawal restored the exact `100000.00` starting balance, and finally-path reconciliation confirmed it |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development actor backend and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured the typed fund baseline, and armed the shared exact-ID command observer before mutation. The partial fund slice selected the synthetic market-data source because G2-027 through G2-029 do not test a feed; the Fund UI, NATS actors and events, Scylla persistence, and typed queries remained the real Development implementations.

For G2-027, the run reused retained fund `2201`, `IFM G2 Automation Fund`. The typed query proved that the fund is non-production and began at balance `100000.00`; the real Fund Transactions editor selected the same named fund and displayed `$100,000.00`. The fixture already contained four immutable, previously compensated test rows from diagnostic runs. Those rows were retained as append-only history, their paired balances were reconciled, and no current-run description existed before mutation.

For G2-028, the real cash-deposit modal submitted `123.45` with description `G2-7187833A-CashDeposit`. `FundTransactionEvent` and `FundTransactionCreatedCompleteEvent` correlated under command ID `96fb5086-9f5c-41c3-957c-9c6882e1e834`. The durable transaction received ID `158101`, used value date `2026-08-19`, retained zero order/trade identifiers because standalone cash movement is not trade-linked, and stored balance `100123.45`. The refreshed editor displayed that row exactly once and showed `$100,123.45`.

For G2-029, the same editor submitted an equal `123.45` withdrawal with description `G2-7187833A-CashWithdrawal`. Its source and successful terminal event correlated under command ID `67f40fb4-0a21-4c5a-8d94-4750a5ea1c8e`; durable transaction ID `158102` stored the restored balance `100000.00`. Typed queries and refreshed UI state contained both current-run append-only rows exactly once and agreed on the restored starting balance.

The cash editor starts its terminal listener before submission, accepts only a complete/fail event with the exact command ID, closes synchronously after success so the parent modal loop can return, and disposes its listener from `FormClosed`. The Fund validator now requires positive order/trade identifiers only for trade-linked transaction types; cash deposits, withdrawals, and their adjustments intentionally use zero. Focused tests cover payload intent, value-date acquisition, exact correlation, unrelated and early terminal events, failure/retry, both validation branches, selector accessibility, listener routes, and environment-override precedence.

No compensating cleanup command was required after the accepted run because G2-029 itself restored the baseline. The finally path re-queried balance `100000.00`, then the Fund window, listeners, desktop, and harness-owned API stopped normally. If a future run is interrupted after its deposit, the harness submits the uniquely described missing withdrawal through the public Fund command API and verifies the restored balance; it never writes storage directly.

This decision accepts G2-027 through G2-029 in addition to the prior accepted G2-001 through G2-026 slices. It does not claim G2-030 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, and cleanup evidence remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-224604-7187833ac9354523bb3bcba7733129a7-Development/`
