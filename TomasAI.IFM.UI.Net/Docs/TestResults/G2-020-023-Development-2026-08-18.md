# G2-020-023 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-020` through `G2-023`: manual economic-calendar maintenance and FMP import |
| Decision | **Passed** |
| Run ID | `20260818-181651-ba7ec8589e974113a311ae735fd19a39` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-020 through G2-023: 11 passed, 0 failed, 0 blocked |
| Process-test duration | 1 minute 41 seconds |
| FMP result | Production adapter returned 23 canonical US rows for `2026-07-17` |
| Cleanup | Succeeded; public MarketData domain removals restored both captured empty baselines |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development API and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured the exact manual/import date-and-country baselines, and armed the shared command-event observer before mutation. The isolated slice configured the unrelated Databento tick source as synthetic so live-feed availability could not obscure the economic-calendar/FMP result; it retained the production FMP adapter and required `FMP_API_KEY` in the API Server environment.

The real economic-calendar editor added isolated row `2036-09-05 04:00:00Z / US / G2-BA7EC858-Calendar` with values `1.01`, `1.02`, and `1.03`; changed those values to `2.01`, `2.02`, and `2.03`; and removed the row. Every operation emitted one source event and one successful terminal event carrying the exact initiating command ID. Bounded typed NATS queries proved every durable transition, and the editor retained the selected date/country and rendered the refreshed row or absence after completion.

For G2-023, UI Automation selected import date `2026-07-17` and country `US` before invoking Import. The parameter-only command reached the economic-calendar MarketData domain actor, whose import-event handler acquired through `IReferenceDataApi`. Production FMP returned 23 canonical rows; `EconomicCalendarsImportedCompleteEvent` carried those rows under the exact command ID, the bounded durable query matched them including provenance metadata, and the refreshed editor rendered the selected date/country and all 23 events. The same assertion accepts a successful canonical zero-row response without treating it as a failure.

Cleanup removed all 23 provider rows through public economic-calendar MarketData domain commands and restored the empty baseline captured before the run. The manual fixture was already absent after G2-022. Command listeners, the Reference window, desktop, and harness-owned API stopped normally, and cleanup succeeded.

This decision accepts G2-020 through G2-023 in addition to the prior accepted G2-001 through G2-019 slices. It does not claim G2-024 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, cleanup evidence, and the machine-readable result remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-181651-ba7ec8589e974113a311ae735fd19a39-Development/`
