# G2-016-019 accepted result - Development - 2026-08-18

| Field | Result |
|---|---|
| Gate slice | `G2-016` through `G2-019`: manual yield-curve maintenance and FMP treasury import |
| Decision | **Passed** |
| Run ID | `20260818-170357-a09e681e82d84bab8fb514a427f358ef` |
| Environment | `Development` |
| Automated result | G2-001 through G2-007 plus G2-016 through G2-019: 11 passed, 0 failed, 0 blocked |
| Process-test duration | 2 minutes 53 seconds |
| FMP result | Production adapter returned one canonical row for `2026-07-17` |
| Cleanup | Succeeded; public domain removal restored the captured empty import-date baseline |
| Source revision | The commit containing this summary; evidence was produced from its pre-commit worktree |

The accepted run started the real Development API and WinForms desktop, reached Healthy readiness with 90 registered actor types, captured the exact manual/import-date baselines, and armed the shared command-event observer before mutation. The isolated slice configured the unrelated Databento tick source as synthetic so live feed availability could not obscure the yield-curve/FMP result; it retained the production FMP adapter and required the environment credential.

The real yield-curve editor added isolated row `2038-09-16` with rates `1.01` through `1.12`, changed it to rates `2.01` through `2.12`, and removed it. Each operation produced one source event and one successful terminal event carrying the exact initiating command ID. Typed NATS queries proved every durable transition, and the editor selected the affected year and rendered the refreshed row or absence after each operation.

For G2-019, UI Automation selected import date `2026-07-17` in the editor before invoking Import. The parameter-only command reached the yield-curve domain actor, whose event-family handler acquired through `IReferenceDataApi`. Production FMP returned one canonical row; `YieldCurveRatesImportedCompleteEvent` carried that row under the exact command ID, the durable exact-date query matched it, and the refreshed editor rendered the imported date. The same assertion accepts a successful canonical zero-row response without treating it as a failure.

Cleanup removed the imported row through the public yield-curve domain command and restored the baseline captured before the run. The manual fixture was already absent after G2-018. Command listeners, the Market Data window, desktop, and harness-owned API then stopped normally, and cleanup succeeded.

This decision accepts G2-016 through G2-019 in addition to the prior accepted G2-001 through G2-015 slices. It does not claim G2-020 through G2-038. Raw screenshots, UI Automation trees, logs, typed query snapshots, exact source/terminal events, cleanup evidence, and the machine-readable result remain in the ignored run directory:

`TomasAI.IFM.UI.Net.SystemTests/TestResults/Runs/20260818-170357-a09e681e82d84bab8fb514a427f358ef-Development/`
