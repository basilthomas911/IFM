# Human-readable release used by the architecture specification.
set(IFM_DATABENTO_VERSION "v0.62.1")

# Immutable commit peeled from the annotated v0.62.1 tag. FetchContent uses
# the commit rather than the tag name so the resolved source cannot move.
set(IFM_DATABENTO_COMMIT "a37965590f6776ac9659ff496f91fb16c81f76b3")

# Annotated tag object retained for provenance and independent verification.
set(IFM_DATABENTO_TAG_OBJECT "930624f0987a81bf6e64478055d33e0fbc049af1")
