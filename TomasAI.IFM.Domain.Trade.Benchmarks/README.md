# Domain.Trade benchmarks

Run all benchmarks from the repository root:

```powershell
dotnet run -c Release --project TomasAI.IFM.Domain.Trade.Benchmarks -- --filter *
```

The maintained summary is in `TomasAI.IFM.Domain.Trade/Docs/Domain-Actor-Optimization-Details.md`.
